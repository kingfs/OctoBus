{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "host": {
      "type": "string",
      "description": "Hillstone management base URL with scheme and port, for example https://203.0.113.10:8443."
    },
    "restBaseUrl": {
      "type": "string",
      "description": "Legacy alias for host."
    },
    "rest_base_url": {
      "type": "string",
      "description": "Alias for host."
    },
    "baseUrl": {
      "type": "string",
      "description": "Legacy alias for host."
    },
    "base_url": {
      "type": "string",
      "description": "Alias for host."
    },
    "endpoint": {
      "type": "string",
      "description": "Alias for host."
    },
    "username": {
      "type": "string",
      "description": "Hillstone login username."
    },
    "userName": {
      "type": "string",
      "description": "Legacy alias for username."
    },
    "user": {
      "type": "string",
      "description": "Alias for username."
    },
    "timeoutMs": {
      "type": "integer",
      "minimum": 1,
      "default": 1500,
      "description": "HTTP timeout in milliseconds."
    },
    "timeout_ms": {
      "type": "integer",
      "minimum": 1,
      "description": "Alias for timeoutMs."
    },
    "skipTlsVerify": {
      "type": "boolean",
      "default": false,
      "description": "Skip TLS certificate verification for private deployments."
    },
    "tlsInsecureSkipVerify": {
      "type": "boolean",
      "default": false,
      "description": "Legacy alias for skipTlsVerify."
    },
    "insecureSkipVerify": {
      "type": "boolean",
      "default": false,
      "description": "Legacy alias for skipTlsVerify."
    },
    "headers": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Optional additional HTTP headers."
    }
  }
}
