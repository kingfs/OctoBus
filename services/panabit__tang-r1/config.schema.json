{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "restBaseUrl": {
      "type": "string",
      "description": "Panabit base URL with http(s) scheme."
    },
    "baseUrl": {
      "type": "string",
      "description": "Alias for restBaseUrl."
    },
    "rest_base_url": {
      "type": "string",
      "description": "Alias for restBaseUrl."
    },
    "base_url": {
      "type": "string",
      "description": "Alias for restBaseUrl."
    },
    "host": {
      "type": "string",
      "description": "Alias for restBaseUrl."
    },
    "bindUser": {
      "type": "string",
      "description": "Login username. Prefer secret.bindUser for persistent credentials."
    },
    "bind_user": {
      "type": "string",
      "description": "Alias for bindUser."
    },
    "user": {
      "type": "string",
      "description": "Alias for bindUser."
    },
    "username": {
      "type": "string",
      "description": "Alias for bindUser."
    },
    "timeoutMs": {
      "type": "integer",
      "minimum": 1,
      "default": 1500,
      "description": "HTTP timeout in milliseconds."
    },
    "timeout_ms": {
      "type": "integer",
      "minimum": 1,
      "description": "Alias for timeoutMs."
    },
    "skipTlsVerify": {
      "type": "boolean",
      "default": false,
      "description": "Skip TLS certificate verification for private deployments."
    },
    "tlsInsecureSkipVerify": {
      "type": "boolean",
      "default": false,
      "description": "Legacy alias for skipTlsVerify."
    },
    "insecureSkipVerify": {
      "type": "boolean",
      "default": false,
      "description": "Alias for skipTlsVerify."
    },
    "headers": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Optional additional HTTP headers."
    }
  }
}
