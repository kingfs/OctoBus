{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "host": {
      "type": "string",
      "description": "DPtech EDS base URL, for example https://eds.example.net:8443."
    },
    "baseUrl": {
      "type": "string",
      "description": "Alias for host."
    },
    "base_url": {
      "type": "string",
      "description": "Alias for host."
    },
    "restBaseUrl": {
      "type": "string",
      "description": "Alias for host."
    },
    "rest_base_url": {
      "type": "string",
      "description": "Alias for host."
    },
    "url": {
      "type": "string",
      "description": "Alias for host."
    },
    "endpoint": {
      "type": "string",
      "description": "Alias for host."
    },
    "user": {
      "type": "string",
      "description": "DPtech EDS Basic Auth username. Prefer secret.user for new instances."
    },
    "username": {
      "type": "string",
      "description": "Alias for user. Prefer secret.username for new instances."
    },
    "account": {
      "type": "string",
      "description": "Alias for user."
    },
    "timeoutMs": {
      "type": "integer",
      "minimum": 1,
      "default": 5000,
      "description": "HTTP timeout in milliseconds."
    },
    "timeout_ms": {
      "type": "integer",
      "minimum": 1,
      "description": "Legacy alias for timeoutMs."
    },
    "skipTlsVerify": {
      "type": "boolean",
      "default": false,
      "description": "Skip TLS certificate verification for private deployments."
    },
    "tlsInsecureSkipVerify": {
      "type": "boolean",
      "default": false,
      "description": "Legacy alias for skipTlsVerify."
    },
    "headers": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Optional additional HTTP headers. Authorization and JSON headers are set by the service."
    }
  }
}
